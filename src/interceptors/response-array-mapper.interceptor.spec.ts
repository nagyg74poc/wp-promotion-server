import { ResponseArrayMapperInterceptor } from './response-array-mapper.interceptor';

describe('ResponseArrayMapperInterceptor', () => {
  it('should be defined', () => {
    expect(new ResponseArrayMapperInterceptor()).toBeTruthy();
  });
});
